( ' dup = ) $True
( 'id ( drop True ) = ) $False

( False True ? ) $not

# ( 'id ( drop False ) ? ! ) $and
# ( ( drop True ) 'id ? ! ) $or
( 'not ' ? ! ) $xor
( ' ? ! ) $test

# ( 1->1 $a 0 a ) $test2

True 'not test

# 'not test2

# True
# False
# True not False =
# False not True =
# True True and True =
# True False and False =

